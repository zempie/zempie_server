export enum eTimeline {
    PR = 1,
    PRW,
    Share,
    Achievement,
    Battle_1st
}

export enum eNotify {
    Follow,
    Reply,
    Like,
    Beaten
}

export enum eNotice {
    Stable,
    Once,
    Event,
}

export enum eRedis {
    Geo,
    Hashes,
    Lists,
    Sets,
    Strings,
}