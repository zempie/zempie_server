export enum eTimeline {
    PR = 1,
    PRW,
    Share,
    Achievement,
    Battle_1st,
}

export enum eNotify {
    Alarm,
    Battle,
    Beat,
    Follow,
    Like,
    Reply,
}

export enum eAlarm {
    Follow,
    Reply,
    Like,
    Beaten,
}

export enum eNotice {
    Stable,
    Once,
    Event,
}

export enum eRedis {
    Geo,
    Hashes,
    Lists,
    Sets,
    Strings,
}

export enum eAppTheme {
    Default,
    Dark,
    ColorWeakness,
    Event,
}

export enum eAppLang {
    KO,
    EN,
}